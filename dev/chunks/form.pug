.form-wrap.offset
	.wrapper
		.form
			form(actions="/", method="GET")
				input(type="text" 
					placeholder="Имя..."
					minlength=2
					required
					name="name")
				input(type="tel"
					placeholder="Телефон..."
					required
					tel="tel"
					pattern="\+7\-[0-9]{3}\-[0-9]{3}\-[0-9]{2}\-[0-9]{2}")
				button.button(type="submit") ОСТАВИТЬ ЗАЯВКУ